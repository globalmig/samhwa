BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[email_logs] ADD [extra_data] NVARCHAR(MAX);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
